# Halyk Voice Router

- Python services are async and must retain an offline-capable routing mode.
- Keep WebSocket trace payloads backward compatible.
- Run a focused router or FastAPI validation after backend edits.
